#include "CppWordAnalyzer.h"

bool
language::translation::CppWordAnalyzer::analyze(fstream streamin, fstream &streamout){
    if(!streamout.is_open()){
        return false;
    }
    /* Analyze here */
}